<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Verify OTP</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex justify-center items-center min-h-screen">

  <div class="bg-white shadow-lg rounded-2xl p-8 w-full max-w-md">
    <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Verify OTP</h2>
    <form action="/auth/verify" method="POST" class="space-y-4">
      
      <div>
        <label class="block text-gray-600 font-medium mb-2">Enter OTP</label>
        <input type="number" name="otp" placeholder="6-digit OTP" required
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:outline-none">
      </div>

      <input type="hidden" name="email" value="<%= email %>">

      <button type="submit"
        class="w-full bg-green-500 hover:bg-green-600 text-white font-semibold py-2 rounded-lg transition">
        Verify
      </button>
    </form>

    <p class="text-center text-sm text-gray-500 mt-4">Didn't receive OTP? <a href="/auth/register" class="text-green-600 font-semibold">Resend</a></p>
  </div>

</body>
</html>
